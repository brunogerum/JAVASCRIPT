var num = [5, 2, 8, 9, 3, 6, 7, 4 ]
num.push(1)
// num.sort()
console.log(num)
console.log(`nosso valor é ${num.length} posições`)
console.log(num[1])

for (let pos = 0; pos < num.length; pos++) {
    console.log (`a posição ${pos} tem o valor de ${num[pos]}`)
    
}

for (let pos in num) {
    console.log (`a posição ${pos} tem o valor de ${num[pos]}`)            
    }*/
